# Copie para .env e ajuste os valores:
# cp .env.example .env

SECRET_KEY=django-insecure-ke@qcdurztpm-*7#wfvo28^kg(e*m@iq61$mfeb#l9myn45b+8

# Frontend Vue (link "Ver site" no Django Admin)
FRONTEND_URL=http://localhost:5173

# sqlite | oracle
DB_ENGINE=oracle

# --- Oracle Cloud Autonomous Database ---
# Alias do tnsnames.ora (confira apos baixar o wallet)
ORACLE_NAME=gerenciamentodemuseu1_high
# Ou use a connection string TCPS completa (alias acima ou string abaixo):
# ORACLE_NAME=(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.sa-saopaulo-1.oraclecloud.com))(connect_data=(service_name=gbe06e8c506932e_gerenciamentodemuseu1_high.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))
ORACLE_USER=admin
ORACLE_PASSWORD=sua_senha_aqui
ORACLE_WALLET_DIR=wallet

# --- Oracle local XE (alternativa) ---
# ORACLE_NAME=localhost:1521/XEPDB1
# ORACLE_USER=museu
# ORACLE_PASSWORD=sua_senha
# ORACLE_WALLET_DIR=
